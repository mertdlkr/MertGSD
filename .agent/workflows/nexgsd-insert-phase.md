---
description: Insert an urgent decimal phase between existing phases
---

# NexGsd Insert Phase

Insert urgent work without destroying roadmap order.

## Steps

1. Read `ROADMAP.md`.
2. Determine the decimal phase slot after the requested phase.
3. Add the inserted phase with `(INSERTED)` marker.
4. Keep dependency logic explicit.
5. Update `STATE.md` if current execution order changes.

## Completion

Show the inserted phase number and why it was added.
